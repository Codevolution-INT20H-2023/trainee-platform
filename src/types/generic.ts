export enum LOCAL_STORAGE_KEYS {
  ACCESS_TOKEN = 'ACCESS_TOKEN',
  REFRESH_TOKEN = 'REFRESH_TOKEN',
}

export enum ROUTES {
  LOGIN = '/login',
  SIGNUP = '/signup',
  PROJECTS = '/projects',
  ACCOUNT = '/account',
  PROFILES = '/profiles',
  HOME = '/',
}
